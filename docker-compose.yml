services:
  app:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next      
    ports:
      - 3000:3000
    environment:
      - WATCHPACK_POLLING=true
    env_file:
      - .env.local