"use client"

import { useState } from "react"
type CalendarProps = {
  currentDate: Date
  setCurrentDate: React.Dispatch<React.SetStateAction<Date>>
}

const ScheduleView = (props: CalendarProps) => {
  return (
      <div className="flex flex-col">
        
      </div>
  )
}

export default ScheduleView
